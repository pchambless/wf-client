import Accounts from './Accounts';
import Users from './Users';
import MeasUnits from './measUnits';  // note: match the file's casing exactly

export const columnMap = {
  accounts: Accounts,
  users: Users,
  measUnits: MeasUnits
};
